import { Box } from '@mui/material';
import OrdersTable from '../components/Table';

const TablePage = () => {
  return (
    <Box>
      <OrdersTable />
    </Box>
  );
};

export default TablePage;
